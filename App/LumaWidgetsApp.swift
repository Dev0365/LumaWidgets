import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 20) {
            Text("LumaWidgets")
                .font(.largeTitle)
                .bold()

            Text("Calm Productivity App")
                .foregroundColor(.gray)
        }
    }
}
